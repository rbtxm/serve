# Tomcat
server: 
  port: 9091

# Spring
spring: 
  application:
    # 应用名称
    name: auth
  profiles:
    # 环境配置
    active: dev
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: 10.211.55.4:8848
        namespace: ${spring.profiles.active}
      config:
        server-addr: 10.211.55.4:8848
        namespace: ${spring.profiles.active}
        file-extension: yaml
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}


logging:
  file:
    name: logs/auth/all_in_one.log